import Vue from 'vue'
import Vuex from 'vuex'
import transfer from './transfer'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    transfer
  }
})
